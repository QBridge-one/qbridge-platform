import { Card, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function OpsIssuerReviewPage() {
  return (
    <div className="space-y-6 p-6">
      <h1 className="text-2xl font-bold tracking-tight">Issuer review</h1>
      <Card>
        <CardHeader>
          <CardTitle>Onboarding queue</CardTitle>
          <CardDescription>KYB / approval workflow for new issuers — placeholder.</CardDescription>
        </CardHeader>
      </Card>
    </div>
  );
}
