// ============================================================
// AUTO-GENERATED — DO NOT EDIT MANUALLY
// Generated by: scripts/generate.ts
// To regenerate: yarn generate
// ============================================================

"use client";

import { useCallback, useState } from "react";
import { useChainId } from "wagmi";
import { transactionService } from "../../../container";
import { useContractAddress } from "../../../hooks/useContracts";
import type { Address } from "../../../core/types";
import { TOKEN_REGISTRY_ABI } from "../abi";

export type RelistTokenArgs = {
  token: `0x${string}`;
};

export function useRelistToken() {
  const [isLoading, setIsLoading] = useState(false);
  const [error, setError] = useState<Error | null>(null);
  const contractAddress = useContractAddress("tokenRegistry") as Address | null;
  const chainId = useChainId();

  const relistToken = useCallback(
    async (args: RelistTokenArgs) => {
      if (!contractAddress) {
        const err = new Error("Contract address is not configured for this chain.");
        setError(err);
        throw err;
      }
      setIsLoading(true);
      setError(null);
      try {
        const result = await transactionService.execute({
          contractCall: {
            address: contractAddress,
            abi: TOKEN_REGISTRY_ABI,
            functionName: "relistToken",
            args: [args.token],
            chainId,
          },
        });
        if (result.status === "failed" && result.error) {
          setError(result.error);
          throw result.error;
        }
        return result.hash;
      } finally {
        setIsLoading(false);
      }
    },
    [contractAddress, chainId],
  );

  const reset = useCallback(() => setError(null), []);

  return { relistToken, isLoading, error, reset };
}
