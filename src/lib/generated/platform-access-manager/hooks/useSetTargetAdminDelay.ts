// ============================================================
// AUTO-GENERATED — DO NOT EDIT MANUALLY
// Generated by: scripts/generate.ts
// To regenerate: yarn generate
// ============================================================

"use client";

import { useCallback, useState } from "react";
import { useChainId } from "wagmi";
import { transactionService } from "../../../container";
import { useContractAddress } from "../../../hooks/useContracts";
import type { Address } from "../../../core/types";
import { PLATFORM_ACCESS_MANAGER_ABI } from "../abi";

export type SetTargetAdminDelayArgs = {
  target: `0x${string}`;
  newDelay: number;
};

export function useSetTargetAdminDelay() {
  const [isLoading, setIsLoading] = useState(false);
  const [error, setError] = useState<Error | null>(null);
  const contractAddress = useContractAddress("platformAccessManager") as Address | null;
  const chainId = useChainId();

  const setTargetAdminDelay = useCallback(
    async (args: SetTargetAdminDelayArgs) => {
      if (!contractAddress) {
        const err = new Error("Contract address is not configured for this chain.");
        setError(err);
        throw err;
      }
      setIsLoading(true);
      setError(null);
      try {
        const result = await transactionService.execute({
          contractCall: {
            address: contractAddress,
            abi: PLATFORM_ACCESS_MANAGER_ABI,
            functionName: "setTargetAdminDelay",
            args: [args.target, args.newDelay],
            chainId,
          },
        });
        if (result.status === "failed" && result.error) {
          setError(result.error);
          throw result.error;
        }
        return result.hash;
      } finally {
        setIsLoading(false);
      }
    },
    [contractAddress, chainId],
  );

  const reset = useCallback(() => setError(null), []);

  return { setTargetAdminDelay, isLoading, error, reset };
}
