// ============================================================
// AUTO-GENERATED — DO NOT EDIT MANUALLY
// Generated by: scripts/generate.ts
// To regenerate: yarn generate
// ============================================================

"use client";

import { useCallback, useState } from "react";
import { useChainId } from "wagmi";
import { transactionService } from "../../../container";
import { useContractAddress } from "../../../hooks/useContracts";
import type { Address } from "../../../core/types";
import { ISSUER_REGISTRY_ABI } from "../abi";

export type MigrateWalletArgs = {
  entityId: `0x${string}`;
  newWallet: `0x${string}`;
  newKybDocumentHash: `0x${string}`;
};

export function useMigrateWallet() {
  const [isLoading, setIsLoading] = useState(false);
  const [error, setError] = useState<Error | null>(null);
  const contractAddress = useContractAddress("issuerRegistry") as Address | null;
  const chainId = useChainId();

  const migrateWallet = useCallback(
    async (args: MigrateWalletArgs) => {
      if (!contractAddress) {
        const err = new Error("Contract address is not configured for this chain.");
        setError(err);
        throw err;
      }
      setIsLoading(true);
      setError(null);
      try {
        const result = await transactionService.execute({
          contractCall: {
            address: contractAddress,
            abi: ISSUER_REGISTRY_ABI,
            functionName: "migrateWallet",
            args: [args.entityId, args.newWallet, args.newKybDocumentHash],
            chainId,
          },
        });
        if (result.status === "failed" && result.error) {
          setError(result.error);
          throw result.error;
        }
        return result.hash;
      } finally {
        setIsLoading(false);
      }
    },
    [contractAddress, chainId],
  );

  const reset = useCallback(() => setError(null), []);

  return { migrateWallet, isLoading, error, reset };
}
