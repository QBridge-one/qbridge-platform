// ============================================================
// AUTO-GENERATED — DO NOT EDIT MANUALLY
// Generated by: scripts/generate.ts
// To regenerate: yarn generate
// ============================================================

"use client";

import { useReadContract, useChainId } from "wagmi";
import { useContractAddress } from "../../../hooks/useContracts";
import type { Address } from "../../../core/types";
import { RESERVE_ORACLE_ABI } from "../abi";

export function useIsStale(
  contractAddress: Address | null | undefined,
  maxAgeSeconds: bigint,
  chainId?: number
) {
  const registryAddress = useContractAddress("reserveOracle") as Address | null;
  const address = (contractAddress ?? registryAddress) ?? undefined;
  const wagmiChainId = useChainId();
  const resolvedChainId = chainId ?? wagmiChainId;

  return useReadContract({
    address,
    abi: RESERVE_ORACLE_ABI,
    functionName: "isStale",
    args: [maxAgeSeconds],
    chainId: resolvedChainId,
    query: { enabled: !!address },
  });
}
