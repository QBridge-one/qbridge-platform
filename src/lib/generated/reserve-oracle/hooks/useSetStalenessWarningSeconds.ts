// ============================================================
// AUTO-GENERATED — DO NOT EDIT MANUALLY
// Generated by: scripts/generate.ts
// To regenerate: yarn generate
// ============================================================

"use client";

import { useCallback, useState } from "react";
import { useChainId } from "wagmi";
import { transactionService } from "../../../container";
import { useContractAddress } from "../../../hooks/useContracts";
import type { Address } from "../../../core/types";
import { RESERVE_ORACLE_ABI } from "../abi";

export type SetStalenessWarningSecondsArgs = {
  newSeconds: bigint;
};

export function useSetStalenessWarningSeconds() {
  const [isLoading, setIsLoading] = useState(false);
  const [error, setError] = useState<Error | null>(null);
  const contractAddress = useContractAddress("reserveOracle") as Address | null;
  const chainId = useChainId();

  const setStalenessWarningSeconds = useCallback(
    async (args: SetStalenessWarningSecondsArgs) => {
      if (!contractAddress) {
        const err = new Error("Contract address is not configured for this chain.");
        setError(err);
        throw err;
      }
      setIsLoading(true);
      setError(null);
      try {
        const result = await transactionService.execute({
          contractCall: {
            address: contractAddress,
            abi: RESERVE_ORACLE_ABI,
            functionName: "setStalenessWarningSeconds",
            args: [args.newSeconds],
            chainId,
          },
        });
        if (result.status === "failed" && result.error) {
          setError(result.error);
          throw result.error;
        }
        return result.hash;
      } finally {
        setIsLoading(false);
      }
    },
    [contractAddress, chainId],
  );

  const reset = useCallback(() => setError(null), []);

  return { setStalenessWarningSeconds, isLoading, error, reset };
}
