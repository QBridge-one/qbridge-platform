# Railway: chain-role indexer only (Node 22). Next.js stays on Vercel.
FROM node:22-bookworm-slim AS runner
WORKDIR /app

COPY package.json yarn.lock ./
RUN yarn install --frozen-lockfile

COPY . .

ENV NODE_ENV=production
CMD ["yarn", "indexer"]
